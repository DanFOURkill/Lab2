import { NextResponse } from 'next/server';import { prisma } from '@/lib/prisma';import { getInvitation } from '@/lib/data';import { responseSchema } from '@/lib/validation';
export async function POST(req:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;const inv=await getInvitation(slug);if(!inv)return NextResponse.json({error:'not found'},{status:404});const parsed=responseSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:parsed.error.flatten()},{status:400});await prisma.invitationResponse.create({data:{invitationId:inv.id,...parsed.data,userAgent:(req.headers.get('user-agent')||'').slice(0,180),submittedAt:new Date()}});return NextResponse.json({ok:true});}
