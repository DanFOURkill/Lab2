'use client';import { useState } from 'react';import { Button } from '../ui/Button';const labels=['Нет','Точно нет?','Подумай ещё','Может всё-таки да?','Последний шанс'];export function MovingNoButton({onNo}:{onNo:()=>void}){const [n,setN]=useState(0);const move=()=>{if(n<4)setN(n+1)};return <Button variant="secondary" style={{transform:`translate(${n<4?n*18:0}px,${n<4?(n%2?16:-10):0}px)`}} onMouseEnter={move} onTouchStart={move} onClick={()=>n>=4?onNo():move()}>{labels[Math.min(n,4)]}</Button>}
