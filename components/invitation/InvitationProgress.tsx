import type { InvitationStep } from '@/lib/types';const steps:InvitationStep[]=['closed','question','acceptedReaction','date','activity','summary','success'];export function InvitationProgress({step}:{step:InvitationStep}){const idx=Math.max(0,steps.indexOf(step));return <div aria-hidden className="mb-4 h-1.5 rounded-full bg-primarySoft/50"><div className="h-full rounded-full bg-primary transition-all" style={{width:`${((idx+1)/steps.length)*100}%`}}/></div>}
