import type { DraftState, Invitation } from '@/lib/types';import { Button } from '../ui/Button';import { Textarea } from '../ui/Textarea';export function InvitationSummary({invitation,state,onComment,onConfirm,onBack}:{invitation:Invitation;state:DraftState;onComment:(v:string)=>void;onConfirm:()=>void;onBack:()=>void}){const acts=invitation.activities.filter(a=>state.selectedActivities.includes(a.id)).map(a=>a.title).join(', ');return <div><h1 className="font-serif text-5xl">Итак, договорились</h1><dl className="mt-6 space-y-3 rounded-3xl bg-white/60 p-4"><div><dt className="text-muted">Дата</dt><dd>{state.selectedDate}</dd></div><div><dt className="text-muted">Формат</dt><dd>{acts}</dd></div></dl><label className="mt-6 block text-sm font-semibold">Хочешь что-нибудь добавить?</label><Textarea className="mt-2" onChange={e=>onComment(e.target.value)} placeholder="Необязательный комментарий"/><div className="mt-6 flex gap-3"><Button variant="secondary" onClick={onBack}>Назад</Button><Button onClick={onConfirm}>Подтвердить</Button></div></div>}
