import type { Invitation } from '@/lib/types';import { Button } from '../ui/Button';import { MovingNoButton } from './MovingNoButton';export function MainQuestion({invitation,onYes,onNo}:{invitation:Invitation;onYes:()=>void;onNo:()=>void}){return <div className="text-center"><p className="text-muted">{invitation.recipientName},</p><h1 className="mt-3 font-serif text-5xl">{invitation.questionText}</h1><div className="mt-10 flex justify-center gap-3"><Button onClick={onYes}>Да</Button>{invitation.allowDecline&&<MovingNoButton onNo={onNo}/>}</div></div>}
