'use client';import { motion } from 'framer-motion';export function FloatingDecorations(){return <div aria-hidden className="pointer-events-none absolute inset-0 overflow-hidden">{[12,28,44,70].map((l,i)=><motion.span key={l} className="absolute h-2 w-2 rounded-full bg-primarySoft" style={{left:`${l}%`,top:`${20+i*14}%`}} animate={{y:[0,-14,0],opacity:[.25,.6,.25]}} transition={{duration:4+i,repeat:Infinity}} />)}</div>}
