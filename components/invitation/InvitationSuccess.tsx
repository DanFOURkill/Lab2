'use client';import type { DraftState, Invitation } from '@/lib/types';import { Button } from '../ui/Button';import { Download, MessageCircle, Image as ImageIcon } from 'lucide-react';function ics(inv:Invitation,state:DraftState){const date=(state.selectedDate||'').replace(/-/g,'')||'20260724';return `BEGIN:VCALENDAR\nVERSION:2.0\nBEGIN:VEVENT\nSUMMARY:Свидание с ${inv.senderName}\nDTSTART:${date}T180000\nEND:VEVENT\nEND:VCALENDAR`}
export function InvitationSuccess({invitation,state}:{invitation:Invitation;state:DraftState}){const save=()=>{const blob=new Blob([ics(invitation,state)],{type:'text/calendar'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='date.ics';a.click()};return <div id="final-card" className="text-center"><h1 className="font-serif text-5xl">Тогда это официально — у нас свидание</h1><p className="mt-5 text-muted">{invitation.finalText}</p><p className="mt-4">Дата: {state.selectedDate}</p><p>От: {invitation.senderName}</p><div className="mt-8 grid gap-3"><Button onClick={save}><Download className="mr-2 h-4 w-4"/>Добавить событие в календарь</Button>{invitation.contactUrl&&<Button asChild variant="secondary" href={invitation.contactUrl}><MessageCircle className="mr-2 h-4 w-4"/>Написать отправителю</Button>}<Button variant="ghost" onClick={()=>window.print()}><ImageIcon className="mr-2 h-4 w-4"/>Сохранить экран как карточку</Button></div></div>}
