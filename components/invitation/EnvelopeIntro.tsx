'use client';import { motion } from 'framer-motion';import { Button } from '../ui/Button';export function EnvelopeIntro({intro,onOpen}:{intro:string;onOpen:()=>void}){return <div className="text-center"><motion.div className="mx-auto mb-8 h-36 w-52 rounded-3xl border border-line bg-gradient-to-br from-white to-primarySoft/60 shadow-lg" whileHover={{rotate:-1}}><motion.div className="mx-auto h-20 w-40 origin-top rounded-b-3xl bg-primarySoft" animate={{rotateX:[0,18,0]}} transition={{duration:2,repeat:Infinity}}/></motion.div><p className="text-muted">{intro}</p><h1 className="mt-3 font-serif text-5xl">Для тебя есть приглашение</h1><p className="mt-3 text-lg">Откроешь?</p><Button className="mt-8" onClick={onOpen}>Открыть</Button></div>}
