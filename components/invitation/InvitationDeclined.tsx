'use client';import { useEffect } from 'react';import { Button } from '../ui/Button';export function InvitationDeclined({onBack,slug}:{onBack:()=>void;slug:string}){useEffect(()=>{fetch(`/api/invitations/${slug}/respond`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({answer:'declined',selectedActivities:[]})}).catch(()=>undefined)},[slug]);return <div className="text-center"><h1 className="font-serif text-5xl">Ничего страшного.</h1><p className="mt-4 text-muted">Спасибо, что честно ответила.</p><Button className="mt-8" variant="secondary" onClick={onBack}>Вернуться</Button></div>}
