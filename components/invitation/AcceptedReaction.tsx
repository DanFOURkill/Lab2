'use client';import { useEffect,useState } from 'react';import { Button } from '../ui/Button';export function AcceptedReaction({text,onContinue}:{text:string;onContinue:()=>void}){const [ready,setReady]=useState(false);useEffect(()=>{const t=setTimeout(()=>setReady(true),900);return()=>clearTimeout(t)},[]);const parts=text.split('\n').filter(Boolean);return <div className="text-center"><h1 className="font-serif text-5xl">{parts.slice(0,2).join(' ')}</h1>{ready&&<p className="mt-6 text-lg text-muted">{parts.slice(2).join(' ')||'Я очень рад. Тогда осталось выбрать детали.'}</p>}<Button className="mt-10" disabled={!ready} onClick={onContinue}>Продолжить</Button></div>}
