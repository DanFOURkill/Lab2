'use client';import { useFormContext } from 'react-hook-form';import type { InvitationFormValues } from '@/lib/validation';export function ThemeSelector(){const {register}=useFormContext<InvitationFormValues>();return <select className="focus-ring w-full rounded-2xl border border-line p-3" {...register('theme')}><option value="rose">Rose</option><option value="wine">Wine</option><option value="peach">Peach</option></select>}
