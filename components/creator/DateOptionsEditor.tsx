'use client';import { useFieldArray, useFormContext } from 'react-hook-form';import type { InvitationFormValues } from '@/lib/validation';import { Button } from '../ui/Button';import { Input } from '../ui/Input';export function DateOptionsEditor(){const {control,register}=useFormContext<InvitationFormValues>();const {fields,append,remove}=useFieldArray({control,name:'availableDates'});return <div className="grid gap-2">{fields.map((f,i)=><div key={f.id} className="grid grid-cols-[1fr_1fr_auto] gap-2"><Input {...register(`availableDates.${i}.label`)}/><Input {...register(`availableDates.${i}.value`)}/><Button type="button" variant="ghost" onClick={()=>remove(i)}>×</Button></div>)}<Button type="button" variant="secondary" onClick={()=>append({id:crypto.randomUUID(),label:'Новая дата',value:''})}>Добавить дату</Button></div>}
