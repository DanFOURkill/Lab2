'use client';import { useFieldArray, useFormContext } from 'react-hook-form';import type { InvitationFormValues } from '@/lib/validation';import { Button } from '../ui/Button';import { Input } from '../ui/Input';export function ActivityOptionsEditor(){const {control,register}=useFormContext<InvitationFormValues>();const {fields,append,remove}=useFieldArray({control,name:'activities'});return <div className="grid gap-3">{fields.map((f,i)=><div key={f.id} className="rounded-2xl border border-line p-3 grid gap-2"><Input {...register(`activities.${i}.title`)} placeholder="Название"/><Input {...register(`activities.${i}.description`)} placeholder="Описание"/><Input {...register(`activities.${i}.icon`)} placeholder="Emoji"/><Input {...register(`activities.${i}.imageUrl`)} placeholder="Изображение URL"/><Button type="button" variant="ghost" onClick={()=>remove(i)}>Удалить</Button></div>)}<Button type="button" variant="secondary" onClick={()=>append({id:crypto.randomUUID(),title:'Свой вариант',description:'',icon:'💬',imageUrl:''})}>Добавить формат</Button></div>}
